import 'package:flutter/widgets.dart';

class WebContextMenuManager extends StatelessWidget {
  const WebContextMenuManager({super.key, required this.child});

  final Widget child;

  @override
  Widget build(context) => child;
}
